version: '1.0'

services:
  flask:
    commands:
      - pip install -r requirements.txt
      - gunicorn app:app
    environment:
      - FLASK_APP=app.py
      - FLASK_ENV=production
    ports:
      - "80:5000"
