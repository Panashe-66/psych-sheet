version: '1.0'

services:
  flask:
    commands:
      - pip install -r requirements.txt
      - gunicorn app:app --bind 0.0.0.0:5000
    environment:
      - FLASK_APP=app.py
      - FLASK_ENV=production
    ports:
      - "80:5000"
